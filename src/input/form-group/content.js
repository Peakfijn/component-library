import styled from 'styled-components';
import defaultProps from '../../default-props';

const FormGroupContent = styled.span`
	display: block;
	position: relative;
`;

FormGroupContent.defaultProps = defaultProps;

export default FormGroupContent;
